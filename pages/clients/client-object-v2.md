### Client Object (Version 2)

Client objects includes basic user information including fields that are only linked to the
Client Role. For example, `students` where the Client is the `paying_client` of the student. 

